# --macro boisly.AppSettings.configureWith("Config"))
# -D nodejs
--library tink_web
--library tink_serialize
--library  asys
--library fireduck
--library tink_http
-D boisly.appSettings.configFile=config/haxe.config.json

--library tink_websocket

# -D duckfireEnabled
--library why-email

-D analyzer-optimize